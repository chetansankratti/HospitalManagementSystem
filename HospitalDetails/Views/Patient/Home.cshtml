@model List<PatientModelDB>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
    <link rel="stylesheet" href="~/css/Style.css"/>
}
<div class="content">
    <div class="head">
        <h2>Sham Hospital's</h2>
        <img src="@Url.Content("~/hospital.png")" class="logo"/>
    </div>
    <nav>
        <a href="@Url.Action("About", "Patient")" class="home-a">About</a>
        <a href="@Url.Action("DoctorDetails", "Doctor")"  class="home-a">Doctor's Details</a>
        <a href="@Url.Action("AppointmentList", "Patient")"  class="home-a">Appointment List</a>
        <a href="@Url.Action("AddAppointment", "Patient")"  class="home-a">Appointment</a>
        <a href="@Url.Action("PatientLogin", "Patient")"  class="home-a">Patient Login</a>
        <a href="@Url.Action("DoctorLogin", "Doctor")"  class="home-a">Doctor's Login</a>
       
    
    
    
    </nav>
    <img src="@Url.Content("~/manipal.jpeg")"/>
</div>